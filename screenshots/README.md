# Screenshots

Drop PNG/JPG screenshots here with these names (referenced by the root README):

- `dashboard.png` — the main Overview dashboard (stat bento + applications + Gmail card)
- `gmail.png` — Gmail Job Tracker after a scan (classified emails)
- `login.png` — the login / sign-up glass card
- `profile.png` — the AI Identity Hub profile editor

To capture: run the app (`backend: npm start`, `frontend: npm run dev`), open http://localhost:5173, and screenshot each page.
