# Copy to ".env" for local, or set in Vercel's Environment Variables for production.
# Points the React app at your backend. Leave unset for local dev (defaults to localhost:5005).
VITE_API_BASE=https://careerai-backend.onrender.com
